import { clearHandler } from "../src/client/js/clearHandler";

describe("Test the ClearHandler Functionality", () => {
    test("Testing the clearHandler function is defined", () => {
        expect(clearHandler).toBeDefined();
    });
});
