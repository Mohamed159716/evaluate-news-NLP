import { handleNav } from "../src/client/js/navHandler";

describe("Test the nav Handler", () => {
    test("Chick if handleNav is defined", () => {
        expect(handleNav).toBeDefined();
    });
});
