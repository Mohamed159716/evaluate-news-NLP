import { toggleClasses } from "../src/client/js/toggleClasses";

describe("Testing the toggleClasses functionality", () => {
    test("Testing the toggleClasses function is defined", () => {
        expect(toggleClasses).toBeDefined();
    });
});
